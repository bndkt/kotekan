#!/usr/bin/env bun
// --hot
// import path from "node:path";

// const cwd = process.cwd();
// const serverPath = path.join(import.meta.dir, "server.ts");
// console.log(cwd, import.meta.dir, serverPath);

// import { $ } from "bun";
// await $`bun build ${serverPath} --compile --outfile app`;

import "./server";
